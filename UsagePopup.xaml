<Window x:Class="ClaudeUsageTray.UsagePopup"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Claude usage"
        Width="330" SizeToContent="Height"
        WindowStyle="None" ResizeMode="NoResize" ShowInTaskbar="False"
        AllowsTransparency="True" Background="Transparent" Topmost="True">

    <Window.Resources>
        <SolidColorBrush x:Key="Surface" Color="#1F1F1E"/>
        <SolidColorBrush x:Key="Edge" Color="#383734"/>
        <SolidColorBrush x:Key="Text" Color="#EDEBE7"/>
        <SolidColorBrush x:Key="Muted" Color="#8C8981"/>
        <SolidColorBrush x:Key="Track" Color="#3A3937"/>
        <SolidColorBrush x:Key="Accent" Color="#D97757"/>

        <Style x:Key="Label" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource Text}"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
        </Style>
        <Style x:Key="Sub" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource Muted}"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontSize" Value="11.5"/>
        </Style>
    </Window.Resources>

    <Border Background="{StaticResource Surface}" CornerRadius="10"
            BorderBrush="{StaticResource Edge}" BorderThickness="1" Padding="18,16,18,14">
        <StackPanel>

            <!-- Session -->
            <Grid>
                <TextBlock Text="Current session" Style="{StaticResource Label}"/>
                <TextBlock x:Name="SessionPctText" Style="{StaticResource Label}"
                           HorizontalAlignment="Right" Foreground="{StaticResource Muted}"/>
            </Grid>
            <Grid Height="8" Margin="0,7,0,5">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition x:Name="SessionFill" Width="0*"/>
                    <ColumnDefinition x:Name="SessionRest" Width="100*"/>
                </Grid.ColumnDefinitions>
                <Border Grid.Column="0" Background="{StaticResource Accent}" CornerRadius="4"/>
                <Border Grid.Column="1" Background="{StaticResource Track}" CornerRadius="4"/>
            </Grid>
            <TextBlock x:Name="SessionResetText" Style="{StaticResource Sub}"/>

            <!-- Week -->
            <Grid Margin="0,18,0,0">
                <TextBlock Text="Current week (all models)" Style="{StaticResource Label}"/>
                <TextBlock x:Name="WeekPctText" Style="{StaticResource Label}"
                           HorizontalAlignment="Right" Foreground="{StaticResource Muted}"/>
            </Grid>
            <Grid Height="8" Margin="0,7,0,5">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition x:Name="WeekFill" Width="0*"/>
                    <ColumnDefinition x:Name="WeekRest" Width="100*"/>
                </Grid.ColumnDefinitions>
                <Border Grid.Column="0" Background="{StaticResource Accent}" CornerRadius="4"/>
                <Border Grid.Column="1" Background="{StaticResource Track}" CornerRadius="4"/>
            </Grid>
            <TextBlock x:Name="WeekResetText" Style="{StaticResource Sub}"/>

            <!-- Freshness: the file only advances while the Claude desktop app runs,
                 so stale data must be visible rather than silently shown as current. -->
            <Border Margin="0,16,0,0" BorderBrush="{StaticResource Edge}" BorderThickness="0,1,0,0" Padding="0,8,0,0">
                <TextBlock x:Name="FreshnessText" Style="{StaticResource Sub}"/>
            </Border>

        </StackPanel>
    </Border>
</Window>
